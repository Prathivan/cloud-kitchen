# Butterfly Cloud Kitchen -- Python dependencies
#
# Install with:
#   pip install -r requirements.txt

Django>=6.1,<6.2
Pillow>=10.0
psycopg2-binary>=2.9
dj-database-url>=2.1   # parses Render's DATABASE_URL -- see config/settings.py
python-dotenv>=1.0
APScheduler>=3.10
django-apscheduler>=0.6
requests>=2.31   # used by accounts/otp_service.py to call the MSG91 API
whitenoise>=6.6   # serves static files in production (e.g. on Render) -- see config/settings.py
gunicorn>=21.2   # production WSGI server -- runserver is dev-only, Render needs this to actually run the site
